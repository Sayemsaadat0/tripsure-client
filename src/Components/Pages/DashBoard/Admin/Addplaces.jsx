import React from 'react';

const Addplaces = () => {
    return (
        <div>
            <h2>todo add place form with ract hook form and chage the name to destinations and the data will be similar to category json so that we can change the data </h2>
        </div>
    );
};

export default Addplaces;